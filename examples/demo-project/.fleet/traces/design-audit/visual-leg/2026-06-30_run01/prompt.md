# Audit prompt — visual_leg design-level

Design-audit the visual_leg experiment BEFORE implementation. File given:

- docs/prereg/visual_leg.md

Answer: is this the right experiment at all? Check that the counterfactual
mask actually isolates visual grounding, that there is a real kill/void
condition (a control that would falsify the effect), that the headline metric
matches the constitution, and that a held-out plan is declared. This gate does
not touch results — it only decides whether production may be queued.
