---
description: One-command initialization of a disciplined ML research project — scaffold + seven-agent fleet + leader constitution.
---

Invoke the `research-init` skill with the arguments below. It gathers three
answers (project name, field, target venue — supports a `--minimal` variant
for solo projects), scaffolds the project skeleton, installs the fleet
constitution, and prints the one-page cheatsheet. Follow the skill's steps
exactly; do not spawn agents during init.

Arguments: $ARGUMENTS
